19.71 * 0.15 #15% tip on a 19.71 meal

# base r = what u get rn. u can use a lot of packages for more funcitonality

install.packages("dslabs") #install package (only needed once)

library(dslabs) #load package


c("tinyverse", "dslabs") #this is a character vector; 

# we can install many packages at once by passing in a character vector 

install.packages(c("tinyverse", "dslabs"))

# can also install packages in rstudio: packages (lower right corner window) > install > type package names > press install button

installed.packages() # shows all the packages installed

