# Plan your data entry, be consistent
# Clear names without spaces (underscores or dashes instead)
# Use YYYY-MM-DD format (ISO 8601 standard)
# No empty cells; use codes for missing data
# One thing per cell
# All rectangular (all rows same length)
# Data dictionary to explain columns and codes.
# No calculations in raw data files; perform in scripts instead.
# No font color or highlighting as data; wont be read
# Regularly back up your data
# Save data as CSV or text files