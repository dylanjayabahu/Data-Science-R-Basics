# Data files: either text (ASCII/Unicode) or binary (e.g., Excel, images)
  # Text files (like CSV) can be viewed in text editors (binary cant)

# Prefer text-based datasets; easier import into R.

# Note: ASCII and Unicode are different encodings; this can affect file reading across languages.








